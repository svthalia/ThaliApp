import {StyleSheet} from 'react-native';

let magenta = '#E62272';

var styles = StyleSheet.create({
  body : {
    backgroundColor: magenta,
    flex: 1
  },
  bodyText: {
    fontFamily: 'opensans',
  }
});

export {styles};
