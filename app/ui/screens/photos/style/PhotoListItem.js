import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  touchable: {
  },
  image: {
  },
  overlayGradient: {
    position: 'absolute',
    top: '50%',
    bottom: 0,
    left: 0,
    right: 0,
    opacity: 0.6,
  },
});

export default styles;
